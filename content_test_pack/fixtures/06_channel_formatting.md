# Fixture: Channel Formatting

Uses `tests/fixtures/benchmark/channel_compression.json` — the article's own claim is already hedged: "Some teams reported reduced administrative workload after adopting AI recruiting agents, though results varied and were not independently measured."

**Expected:** LinkedIn/X/newsletter adaptations preserve that hedge; any adaptation that compresses it into a stronger, unqualified claim ("this tool eliminates administrative workload") should be flagged by the channel evaluator's `certaintyInflationDetected` field.
